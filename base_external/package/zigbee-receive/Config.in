
config BR2_PACKAGE_ZIGBEE_RECEIVE
	bool "zigbee-receive"
	help
	  Includes the executable for the Xbee receiver module in the kernel image.
	  Used to understand buildroot external trees and package builds.
